/* DESIGN.md §4.2 / Components §1 — uppercase tracked label with sage square prefix */
export default function EyebrowLabel({ children }: { children: React.ReactNode }) {
  return <p className="eyebrow">{children}</p>;
}
